import RegisterForm from "@/components/register";

export const metadata = {
  title: "Register - Portfolio Panel",
  description: "Register to create your portfolio admin panel account",
  keywords: ["register", "portfolio", "admin", "panel"],
};

export default function RegisterPage() {
  return <RegisterForm />;
}
